import React from "react";
import "./Title.css";

const Title = (props) => {
  return (
    <div className="title">
      <h1>
        Example carousel made only using react core, HTML and CSS and
        styled-components (css-in-js approach)
      </h1>
    </div>
  );
};

export default Title;
